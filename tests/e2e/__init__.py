"""End-to-end suite (TODO.md Phase 4).

Everything under here is marked `@pytest.mark.live` and therefore skipped
by default — see tests/conftest.py's --live opt-in. These tests hit a REAL
database and make REAL (paid) AI calls.
"""
